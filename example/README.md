# Fincra SDK — sample app

A small Flutter app that exercises every flow in `fincra_flutter` against the
*sandbox* environment.

## Run it

```bash
cd example
flutter run
```

From the first screen in main 
you are taken to home where you can navigate to the other screens 


